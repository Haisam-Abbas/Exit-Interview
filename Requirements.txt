Python >= 3.10
pip 

Required Python packages:

torch

transformers

scikit-learn

pandas

openpyxl

tqdm

safetensors 

To install:

pip install torch transformers scikit-learn pandas openpyxl tqdm safetensors

If using Git Large File Storage (LFS) for large model files, install Git LFS too.

GPU recommended but not required.